{% extends 'CLLeagueBundle::homepage.html.twig' %}
{% block body %}
	<div class="text-center">
	
		{{ LeagueEntity.name }}
	</div>
	<div>
		<ul class="nav nav-pills nav-stacked">
			{% for entity in TeamEntities %}
				{{ loop.index }}<li><a href="{{ path('cl_show_team', { 'id': entity.id }) }}">{{ entity.name }}</a></li>		
	
	 	    {% endfor %}
		</ul>
	</div>"

{% endblock body %}