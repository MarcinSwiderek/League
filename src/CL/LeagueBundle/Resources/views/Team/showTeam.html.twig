{% extends 'CLLeagueBundle::homepage.html.twig' %}
{% block body %}
	<div class="text-center">
	
		<h2>{{ TeamEntity.name }}</h2>
	</div>
	<div class="row text-center">
		<img src="{{ TeamEntity.teamPhotoUrl }}" height="500">
	</div>
	<div class="row">
	<!-- dane o drużynie -->
	</div>
	
	<div>
		<ul class="nav nav-pills nav-stacked">
			{% for entity in PlayerEntities %}
				{{ loop.index }}<li><a href="{{ path('cl_show_player', { 'id': entity.id }) }}">{{ entity.firstName }} {{ entity.lastName }} | {{ entity.role }} | {{ entity.birthday | date('Y-m-d') }}
				    </a>
				</li>		
	
	 	    {% endfor %}
		</ul>
	</div>"

{% endblock body %}